---
description: Project scripts and common tasks
globs: ["package.json"]
alwaysApply: false
---
## Project scripts
*Always use pnpm.
- Dev: `pnpm run dev` → Next dev with Turbopack.
- Build: `pnpm run build` → Next build with Turbopack.
- Start: `pnpm run start`.
- Lint: `pnpm run lint` (fails on warnings > 0).
- Test: `pnpm run test` (Bun runner, non-interactive when needed).
